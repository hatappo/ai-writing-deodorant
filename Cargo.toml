[package]
name = "ai-deodorant"
version = "0.1.0"
edition = "2021"
description = "Remove AI-like formatting from text files"
license = "MIT"
repository = "https://github.com/fumi/ai-writing-deodorant"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
regex = "1.12"
